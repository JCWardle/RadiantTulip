<Window x:Class="RadiantTulip.View.GameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:g="clr-namespace:RadiantTulip.View"
        xmlns:VM="clr-namespace:RadiantTulip.View.ViewModels"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        Title="MainWindow" Height="600" Width="1200">
    
    <Window.DataContext>
        <VM:GameViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="200"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <TabControl Grid.Column="0">
            <TabItem Header="Team 1">
                <g:TeamList Players="{Binding Game.Teams[0].Players}" SelectedPlayers="{Binding SelectedPlayers}"/>
            </TabItem>
            <TabItem Header="Team 2"/>
        </TabControl>
        
        <DockPanel Grid.Column="1">
            <StackPanel Orientation="Vertical" DockPanel.Dock="Bottom">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    <Label Name="CurrentTime" Grid.Column="0">
                        <Label.Content>
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0:D2}:{1:D2}:{2:D2}">
                                        <Binding Path="CurrentTime.Minutes"/>
                                        <Binding Path="CurrentTime.Seconds" />
                                        <Binding Path="CurrentTime.Milliseconds"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Label.Content>
                    </Label>
                    <g:DraggableThumbSlider x:Name="PlayHead" 
                        Minimum="0" 
                        Maximum="{Binding RunTime.TotalMilliseconds}" 
                        TickFrequency="100"
                        Grid.Column="1"
                        IsSnapToTickEnabled="True"
                        FinalValue="{Binding CurrentTimeMilliseconds, Mode=TwoWay}"
                        IsMoveToPointEnabled="True"
                        />
                    <Label Name="RunTime" Grid.Column="2">
                        <Label.Content>
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0:D2}:{1:D2}:{2:D2}">
                                        <Binding Path="RunTime.Minutes"/>
                                        <Binding Path="RunTime.Seconds" />
                                        <Binding Path="RunTime.Milliseconds"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Label.Content>
                    </Label>
                </Grid>

                <Button Name="Play" Content="Play">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction Command="{Binding PlayCommand}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
                <Button Name="Stop" Content="Stop">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction Command="{Binding StopCommand}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
            </StackPanel>
            <Border DockPanel.Dock="Top">
                <g:GameDisplay x:Name="GameDisplay" Game="{Binding Game}" CurrentTime="{Binding CurrentTime}" SelectedPlayers="{Binding SelectedPlayers}"/>
            </Border>
        </DockPanel>
        
        <StackPanel Grid.Column="2">
            <g:ContextPanel SelectedPlayers="{Binding SelectedPlayers}"/>
        </StackPanel>
    </Grid>
</Window>
