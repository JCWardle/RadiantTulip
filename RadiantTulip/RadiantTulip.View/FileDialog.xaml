<UserControl x:Class="RadiantTulip.View.FileDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="300">
    <Grid Margin="0, 0, 0, 0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBox Grid.Column="0" Text="{Binding SelectedFile}" HorizontalAlignment="Stretch"/>
        <Button Grid.Column="1" Margin="10, 0, 0, 0" Click="Browse_Click" HorizontalAlignment="Right">Browse...</Button>
    </Grid>
</UserControl>
